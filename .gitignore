# Environments & Packages
.venv/
node_modules/
.next/
__pycache__/
*.pyc

# Local Configs & Executables
.env
.env.local
cloudflared.exe
.idea/
.claude/
edge_profile/

# Databases & Logs
elo.db*
killfeed.db
killfeed.db-shm
killfeed.db-wal
killfeed_log.csv
calibration_cache.json

# Image & Dataset crops
crops/
crops_noise/
wordlevel_crops/
test_crops/
labels/
boundary_frames/
debug_frame.png
debug_bmap.png
colab_bundle.zip
easyocr_lmdb_dataset_ready/
easyocr_lmdb_dataset_ready.zip
easyocr_dataset_staging/
models/

# Beads / Dolt files (added by bd init)
.dolt/
*.db
.beads-credential-key
.beads/proxieddb/

# Regenerable training datasets / LMDB bundles
easyocr_dataset/
easyocr_dataset_v2/
easyocr_dataset_v3/
easyocr_lmdb_*

# Synthetic word-level training data (bead gc3) — regenerable via synth_wordlevel.py / build_wordlevel_lmdb.py
wordlevel_crops_synth/
easyocr_dataset_synth/
easyocr_dataset_synth_ready.zip
labels/labels_wordlevel_synth.csv

# Rolling DB/CSV/JSON backup snapshots (killfeed.db.bak_*, *.csv.bak_*, etc.)
*.bak_*

# Run logs & debug image/array dumps
logs/
scratch/*.png
scratch/*.npy
scratch/*.jpg

# Frontend build cache
*.tsbuildinfo

# Extracted Apex UI fonts (TypeType commercial IP; for local synthetic training only)
fonts/apex/

# Golden-game harness: bulky, regenerable crop images (the JSONL ground truth IS tracked)
tools/golden/data/vod_capture/crops/
tools/golden/data/kf_crops/
tools/golden/data/**/reads.jsonl.orig
